<nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= image_tag("basket.png", :alt => "rss feed", :style => "height:100px") %>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
        <ul class="nav navbar-nav navbar-left">
          <li>
          <% if user_signed_in? %>
            <%= link_to " Log Out", destroy_user_session_path, method: :delete, class:"glyphicon glyphicon-log-out white bold"%>
            <% else %>
            <%= link_to " Register", new_user_registration_path, class:"glyphicon glyphicon-user white bold"%>
            <% end %>
          </li>
            <li>
            <% if user_signed_in? %>
              <%= link_to " My Account", user_path(current_user.id), class:"glyphicon glyphicon-pencil white bold" %>
            <% else %>
              <%= link_to " Log In", new_user_session_path, class:"glyphicon glyphicon-log-in white bold"%>
          <% end %>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Home", root_path, class:"white bold" %></li>
            <li><%= link_to "About", pages_about_path, class:"white bold" %></li>
            <li><%= link_to "Products", products_path, class:"white bold"  %></li>
            <li><%= link_to "Delivery", pages_delivery_path, class:"white bold"  %></li>
            <li><%= link_to "Cart", cart_path, class:"white bold"  %></li>
            <li class="search">
            <%= form_tag(products_path, :method => "get", id: "search-form") do %>
            <%= text_field_tag :search, params[:search], placeholder: "Search products" %>
            <%= submit_tag "Search"%>
          <% end %>
          </li>
        </ul>        
      </div>
    </div>
</nav>